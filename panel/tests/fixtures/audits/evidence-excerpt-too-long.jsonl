{"id": "evidence-oversized", "sessionId": "2026-07-20T18-42-01-abcd1234", "eventRef": "msg_0099#tool_result_9", "agentId": "main", "timestamp": "2026-07-20T18:50:00Z", "eventType": "tool_result", "toolName": "bash", "measured": {"outputTokens": 5200}, "explanation": "Full command output was copied into the excerpt instead of being summarized and digested.", "excerpt": "This tool result excerpt is deliberately padded past the two-thousand character bound so parseEvidenceJsonl must reject it. This tool result excerpt is deliberately padded past the two-thousand character bound so parseEvidenceJsonl must reject it. This tool result excerpt is deliberately padded past the two-thousand character bound so parseEvidenceJsonl must reject it. This tool result excerpt is deliberately padded past the two-thousand character bound so parseEvidenceJsonl must reject it. This tool result excerpt is deliberately padded past the two-thousand character bound so parseEvidenceJsonl must reject it. This tool result excerpt is deliberately padded past the two-thousand character bound so parseEvidenceJsonl must reject it. This tool result excerpt is deliberately padded past the two-thousand character bound so parseEvidenceJsonl must reject it. This tool result excerpt is deliberately padded past the two-thousand character bound so parseEvidenceJsonl must reject it. This tool result excerpt is deliberately padded past the two-thousand character bound so parseEvidenceJsonl must reject it. This tool result excerpt is deliberately padded past the two-thousand character bound so parseEvidenceJsonl must reject it. This tool result excerpt is deliberately padded past the two-thousand character bound so parseEvidenceJsonl must reject it. This tool result excerpt is deliberately padded past the two-thousand character bound so parseEvidenceJsonl must reject it. This tool result excerpt is deliberately padded past the two-thousand character bound so parseEvidenceJsonl must reject it. This tool result excerpt is deliberately padded past the two-thousand character bound so parseEvidenceJsonl must reject it. This tool result excerpt is deliberately padded past the two-thousand character bound so parseEvidenceJsonl must reject it. This tool result excerpt is deliberately padded past the two-thousand character bound so parseEvidenceJsonl must reject it. This tool result excerpt is deliberately padded past the two-thousand character bound so parseEvidenceJsonl must rej", "sourceLocation": "~/.omp/agent/sessions/omp-kanban/2026-07-20T18-42-01-abcd1234.jsonl:210"}
